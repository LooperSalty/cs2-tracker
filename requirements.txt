# Dependances d'execution de l'application de bureau.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pydantic>=2.9.0

# Fenetre native : WebView2 (moteur integre a Windows) + icone systeme.
pywebview>=6.0
pystray>=0.19
pillow>=10.0
